apiVersion: v1
kind: ConfigMap
metadata:
  name: foxclaw-config
  labels:
    app: foxclaw
data:
  foxclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.foxclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "FoxClaw Assistant",
            "workspace": "~/.foxclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # FoxClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
