apiVersion: v1
kind: Service
metadata:
  name: foxclaw
  labels:
    app: foxclaw
spec:
  type: ClusterIP
  selector:
    app: foxclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
