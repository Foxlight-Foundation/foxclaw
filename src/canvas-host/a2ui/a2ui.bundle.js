// Stub A2UI bundle — source directories (vendor/a2ui, apps/shared/OpenClawKit) were removed during FoxClaw fork.
// This placeholder satisfies the build pipeline and runtime asset checks.
// Replace with a real bundle if A2UI canvas support is needed.
console.log("[foxclaw] A2UI canvas: stub bundle loaded — no renderer available.");
